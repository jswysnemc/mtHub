```git
git init							创建本地仓库
git add .							将全部文件添加到追踪
git commit -m "更新信息"			 将追踪文件添加到本地仓库,并追加更新信息
git remote add <remoteName> <url>	添加外置仓库地址
git remote -v						查询remote 信息
git remote remove <name>			删除remote
git push -u <remoteName> master -f	强制推送
git pull		将远程仓库同步到本地
```

